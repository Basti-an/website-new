import { makeStyles } from "@material-ui/core";

export const switchStyles = makeStyles(() => ({
  switch: {
    width: 30,
  },
  switchButton: {
    border: "none",
    outline: "none",
    background: "inherit",
  },
}));
